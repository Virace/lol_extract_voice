# Lol Audio Unpack v3.10.3

修复浅色主题下全局进度条文字不可读，以及任务完成后音频无法选择导出的问题。

- 全局进度条左侧的标题和说明随深浅主题切换文字颜色，浅色主题下准备中和未填充区域的文字恢复可读。
- 任务产物自动刷新后，“选择导出”、多选和“导出此节点下的音频”恢复正常，无需切换页面或重新选择实体。
- 保留实体预览的异步加载和缓存，不增加界面线程的数据读取。

本版包含已撤下的 v3.10.2 的音频导出修复，不改变配置、音频输出布局或内容库格式，无需重新解包。
