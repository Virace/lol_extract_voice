# Lol Audio Unpack 项目约束

## 技术与兼容

- Python 最低版本为 3.10；依赖和命令统一通过 uv 管理。
- 主运行环境是原生 Windows；WSL 支持 CLI、测试与静态检查，GUI 运行优先原生 Windows。
- 核心依赖必须声明在 `pyproject.toml` 并由 `uv.lock` 固定；GUI 与构建依赖保持可选分组。
- 项目采用 `src/` 布局，不为形式迁移为其他目录结构。

## 配置与输入

- 配置文件为 `lol-audio-unpack.ini`；开发模式使用对应 dev 文件。
- 默认值加 CLI 显式参数，与完整 `-c` 配置文件模式互斥，不做隐式逐层 merge。
- 生产配置不读取旧 `LOL_*` 环境变量。测试可使用明确命名的环境变量定位外部测试资源，
  但不能改变应用配置契约。
- 英雄选择支持稳定 ID 与 alias；地图更新必须包含 ID `0` 作为 Common 去重基线。

## 数据与外部契约

- 默认保留原始 WEM 文件及游戏数据中的 ID，不做语义重命名。
- 生产结构化数据使用 MessagePack，开发模式使用 YAML。
- 远端模式信任 `RiotGameData.resolve_live_manifest_pair()` 提供对齐的 LCU/GAME 快照。
- 远端模式优化峰值磁盘而非总耗时；失败时不得静默使用不对齐清单。
- 上游库的内部算法由上游维护；本项目只负责并验证自己的适配、编排、产物和恢复边界。

## 安全、隐私与文件系统

- 代码、项目文档和测试样本不得写入开发者机器的真实绝对路径、凭证或可定位个人环境的信息。
- 用户消息不直接暴露密钥、完整堆栈或不必要的后端实现细节；诊断信息进入内部日志。
- 运行时拒绝越界 BIN 路径；外部输入参与路径或子进程参数时必须保留结构化边界。
- 测试、benchmark、构建 scratch 与缓存统一写入 `.temp/`，任务结束清理本轮可安全识别的产物。

## 测试与发布

- GUI 测试不进入默认离线门禁；精选 pytest-qt 状态测试显式执行，视觉与正式可执行文件由
  人工验收。
- 本地客户端、远端 live 和多进程兼容测试位于 `tests/system/`，默认 pytest 不发现它们。
- 不通过读取源码字符串来证明运行时、GUI、CI 或发布行为正确。
- 默认分支为 `main`，开发分支为 `v3-test`；远端 push、PR、tag、Release 和 merge 均需单独授权。
- 维护变更不隐含版本号、release notes、tag 或发布操作。
