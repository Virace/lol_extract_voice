# Lol Audio Unpack 产品说明

## 目标用户

- 主要用户：需要查找、试听并导出英雄联盟语音和音效的内容创作者，以 GUI 完成人工操作。
- 次级用户：需要批量处理、脚本集成和原始数据的研究者、工具维护者，以 CLI / Python API 完成自动化。
- 需要消费外部工具已准备的客户端目录的高级用户。

## 要解决的问题

游戏音频分散在 LCU、WAD、BIN、BNK 与 WPK 等资源中，并依赖版本、区域、实体与事件关系。
项目以 GUI 为主要用户入口，把数据准备、原始 WEM 解包、WAV 转码和事件 mapping 组织为可重复
执行的工作流。CLI / Python API 继续维护自动化与兼容能力，GUI 优先不改变核心与展示层的职责边界。

## 当前产品方向

- 优先降低“准备数据 → 选择对象与处理范围 → 执行 → 查找结果 → 试听与导出”的理解和操作成本。
- 用户应能从页面判断当前状态、下一步动作和结果位置，不必先理解 BIN、binding、schema 等内部概念。
- 新功能优先进入已有页面职责，保持主要导航、常用动作位置和术语连续；具体稳定约定见
  `PROJECT_CONSTRAINTS.md`，页面与视觉基线见 `PROJECT_DESIGN.md`。
- 当前首先梳理选择、任务与结果之间的衔接，再推进查找/导出增强和长任务恢复；候选功能不等于
  已实现能力或版本承诺。不为展示效果反复重设计界面。
- 本项目方向由软件易用性和真实行为决定。视频脚本、录屏版本、素材冻结和样片制作由独立视频
  项目管理，不作为本项目优化待办；既有软件质量与发布门禁仍然适用。

## 核心能力

- 按英雄稳定 ID、alias 或地图 ID 更新结构化数据。
- 从已安装客户端或外部准备的等价本地目录读取代表资源。
- 解包英雄与地图的 VO、SFX、MUSIC，保留原始 WEM ID。
- 使用 NativeHIRC 或可选 wwiser 生成事件到音频的 mapping。
- 独立执行 WAV 转码，并生成运行报告。
- 通过 CLI 提供可脚本化入口，通过 GUI 提供人工操作、进度、预览和导出。
- 总览支持当前单个实体的音频选择与批量 WAV 导出；任务结果通过常驻通知和覆盖式详情查看，
  按可靠的文件、容器或阶段范围手动重试失败项，不提供跨实体素材篮子或任务续跑。

## 非目标

- 不替代 Riot 客户端、内容分发服务或上游二进制解析库。
- 不承诺把原始音频自动重命名为语义化文件名。
- 事件名与对象 ID 只提供查找线索，不承诺台词识别、中文语义搜索或自动理解音频内容。
- 不把 GUI 视觉质量交给自动化测试判定；稳定逻辑和状态切换可由精选 pytest-qt 测试验证。
- 不提供客户端资源下载器、远端清单解析或网络回退。

## 可验证的成功标准

- GUI 常用流程能明确显示处理对象、动作、当前状态与结果入口；未解包、未生成映射、失败和取消
  可区分，并给出符合实际能力的下一步。易用性由具体操作场景判断，不以页面数量或功能数量衡量。
- 默认离线门禁能够在无游戏客户端、无网络和无 GUI 依赖时通过。
- 显式本地系统门禁覆盖英雄 1、Jade 60009、地图 0/11/22 的资源绑定与 WEM；英雄 1、
  Jade 60009 和地图 11 同时验证 NativeHIRC mapping 与报告。
- 本地数据源预检能在输出初始化前拒绝缺失或损坏目录；已安装客户端与外部准备目录共用合同。
- CLI 与项目文档使用相同的动作式命令和配置模型。
- GUI 发布包由人工完成视觉、启动与完整操作链验收；配置、任务状态、异步和错误恢复同时有
  显式逻辑测试兜底。
